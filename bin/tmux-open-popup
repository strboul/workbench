#!/usr/bin/env bash

# Open a program in tmux popup
#
# Example:
#   tmux-open-popup top
#

PROGRAM="$1"

DIRECTORY="$PWD"
git_dir="$(git rev-parse --show-toplevel 2> /dev/null)"
if [[ ${#git_dir} -gt 0 ]]; then
  DIRECTORY="$git_dir"
fi

tmux popup -w99% -h99% -KE -d "$DIRECTORY" -R "$PROGRAM"
